# ExamGuardrail Agent — Python Dependencies
# Install with: pip install -r requirements.txt

# Core scanning
psutil>=5.9.0
httpx>=0.24.0

# Optional: system tray icon (remove if not needed)
pystray>=0.19.0
Pillow>=10.0.0
